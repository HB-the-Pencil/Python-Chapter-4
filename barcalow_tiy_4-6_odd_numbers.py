# Create a list that counts by 2 from 1 to 20 (aka the odd numbers).
odd_numbers = list(range(1,21,2))

# Loop through the list to print each number.
for number in range(len(odd_numbers)):  # why just use boring old "value" when I can use "number"?
    print(odd_numbers[number])

# I don't actually know how we're supposed to print the lists, but this works, so I'll stick with it.